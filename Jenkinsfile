library identifier: 'mulePipeline@master', retriever: modernSCM([
    $class: 'GitSCMSource',
    credentialsId: 'gitlab_maduma_org',
    remote: 'https://gitlab.maduma.org/maduma/jenkins-mule-pipeline.git'])

mulePipeline()