library identifier: 'mulePipeline@pompiste', retriever: modernSCM([
    $class: 'GitSCMSource',
    credentialsId: 'github',
    remote: 'https://github.com/maduma/mule-pipeline.git'])

mulePipeline()
