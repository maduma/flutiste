library identifier: 'mulePipeline@v0.3', retriever: modernSCM([
    $class: 'GitSCMSource',
    credentialsId: 'github',
    remote: 'https://gitlab.maduma.org/maduma/jenkins-mule-pipeline.git'])

mulePipeline()
